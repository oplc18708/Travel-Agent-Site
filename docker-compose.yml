version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_KEY=changeme
      - FLASK_SECRET_KEY=dev
      - DB_PATH=/app/data/submissions.db
